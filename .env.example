# Copy to .env for local (non-Docker) runs. Never commit the real .env.

# FastAPI
LOG_LEVEL=INFO

# Streamlit dashboard
API_URL=http://127.0.0.1:8000/predict

# MLflow tracking (used by scripts/train.py)
MLFLOW_TRACKING_URI=http://127.0.0.1:5000
